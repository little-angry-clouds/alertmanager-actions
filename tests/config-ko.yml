pyms:
  services:
    metrics: true
  config:
    debug: true
    app_name: alertmanager-actions
    testing: false
alertmanager_actions:
  - name: TestActions
    labels:
      alertname: TestActions
      test: "yes"
